{
  "name": "example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && rm -rf ../../public/subscribers && mkdir -p ../../public/subscribers && cp -r dist/ ../../public/subscribers",
    "preview": "vite preview"
  },
  "dependencies": {
    "subscribers": "file:../"
  },
  "devDependencies": {
    "typescript": "^4.6.4",
    "vite": "^3.2.3"
  }
}
