import { subscribe } from '../state'

describe('subscribe', () => {
    it('runs', () => {
        expect(5).toBe(5)
    })
  // it('subscribes to a property', () => {
  //   const subscriber1 = jest.fn()
  //   subscribe('foo', subscriber1)
  //   expect(p).toEqual(1)
  //   // object.foo = 2
  //   // expect(subscriber1).toHaveBeenCalledWith(2)
  // })
})